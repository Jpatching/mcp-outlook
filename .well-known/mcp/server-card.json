{
  "serverInfo": {
    "name": "patchingjoshua/mcp-outlook",
    "version": "0.1.0"
  },
  "authentication": {
    "required": false
  },
  "tools": [
    {
      "name": "outlook_check_status",
      "description": "Check Microsoft 365 Outlook authentication and connection status",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "outlook_list_messages",
      "description": "List recent emails from an Outlook mail folder",
      "inputSchema": {
        "type": "object",
        "properties": {
          "folder": { "type": "string" },
          "top": { "type": "integer" },
          "unread_only": { "type": "boolean" }
        }
      }
    },
    {
      "name": "outlook_search_messages",
      "description": "Search emails across the mailbox using query terms",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": { "type": "string" },
          "top": { "type": "integer" }
        },
        "required": ["query"]
      }
    },
    {
      "name": "outlook_get_message",
      "description": "Get full details and content of a specific email message by its ID safely encapsulated",
      "inputSchema": {
        "type": "object",
        "properties": {
          "message_id": { "type": "string" }
        },
        "required": ["message_id"]
      }
    },
    {
      "name": "outlook_create_draft",
      "description": "Create an email draft in Outlook Drafts folder",
      "inputSchema": {
        "type": "object",
        "properties": {
          "subject": { "type": "string" },
          "body": { "type": "string" },
          "to_recipients": { "type": "array" }
        },
        "required": ["subject", "body", "to_recipients"]
      }
    },
    {
      "name": "outlook_send_mail",
      "description": "Send an email through Outlook (diverts to Drafts unless confirm_send=True)",
      "inputSchema": {
        "type": "object",
        "properties": {
          "subject": { "type": "string" },
          "body": { "type": "string" },
          "to_recipients": { "type": "array" },
          "confirm_send": { "type": "boolean" }
        },
        "required": ["subject", "body", "to_recipients"]
      }
    },
    {
      "name": "outlook_list_calendar_events",
      "description": "List upcoming calendar events and meetings from Outlook",
      "inputSchema": {
        "type": "object",
        "properties": {
          "days_ahead": { "type": "integer" }
        }
      }
    },
    {
      "name": "outlook_create_calendar_event",
      "description": "Create and schedule a meeting or event in Outlook Calendar",
      "inputSchema": {
        "type": "object",
        "properties": {
          "subject": { "type": "string" },
          "start_datetime": { "type": "string" },
          "end_datetime": { "type": "string" }
        },
        "required": ["subject", "start_datetime", "end_datetime"]
      }
    },
    {
      "name": "outlook_list_folders",
      "description": "List all mail folders in the Outlook account",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    }
  ],
  "resources": [],
  "prompts": []
}
